fastapi
uvicorn
python-multipart
werkzeug
openai-whisper
ffmpeg-python
moviepy
pydub
python-docx
graphviz
fpdf
pymongo
azure-storage-blob
sentence-transformers
transformers
torch
scipy
# --- FastAPI & Server ---
fastapi==0.115.0
uvicorn==0.30.0
python-multipart==0.0.6

# --- Core Utilities ---
pydub==0.25.1
deep-translator==1.11.4
python-docx==1.1.2
graphviz==0.20.3
boto3==1.35.0
httpx==0.27.0
pydantic==2.9.0

# --- AI / OpenAI ---
openai==2.6.1   # ✅ latest SDK that supports client.audio.transcriptions.create()

# --- Logging / General ---
tqdm==4.66.5
requests==2.32.3

# --- Optional (recommended for better async performance) ---
aiofiles==24.1.0
anyio==4.6.0

# --- FFmpeg must be installed system-wide ---
# Ubuntu install command:
# sudo apt install ffmpeg -y
